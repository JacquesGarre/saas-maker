<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            margin-top: 35px;
            background-color: #ffffff;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }
        .header {
            text-align: center;
            background-color: #4f46e5;
            color: #ffffff;
            padding: 20px 0;
            border-radius: 5px;
        }
        .header h1 {
            margin: 0;
        }
        .content {
            padding: 20px;
        }
        .content p {
            line-height: 1.6;
        }
        .button {
            display: block;
            width: 200px;
            margin: 20px auto;
            text-align: center;
            background-color: #4f46e5;
            color: #ffffff;
            padding: 15px;
            text-decoration: none;
            border-radius: 5px;
        }
        .footer {
            text-align: center;
            color: #777;
            padding: 10px 0;
            border-top: 1px solid #e4e4e4;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Welcome to {{ company_name}}, {{ user.first_name }}!</h1>
        </div>
        <div class="content">
            <p>Dear {{ user.first_name }} {{ user.last_name }},</p>
            <p>Thank you for registering with us. To complete your registration and verify your email address, please click the button below:</p>
            <a href="{{ frontend_url }}/verify/{{ user.verification_token }}" class="button">Verify Your Email</a>
            <p>If you did not create an account, please disregard this email.</p>
        </div>
        <div class="footer">
            <p>Thank you,<br>The {{ company_name }} Team</p>
        </div>
    </div>
</body>
</html>